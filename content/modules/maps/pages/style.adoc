= 样式

使用 `Style` 对象可以自定义地图 xref:maps:features-geometries.adoc[要素] 的颜色、大小、字体和其他可视化属性。

样式也可以在 xref:maps:layers-sources.adoc[图层] 级别应用，从而为图层中的所有要素定义一致的外观。

== PointFeature 样式

如需更改 xref:maps:features-geometries.adoc#point-feature[PointFeature] 的样式，可以定义一个 `PointStyle` 对象，设置所需的外观属性（如 `image`、`fill`、`stroke` 等），然后将该样式应用于 `PointFeature`。

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/StyleView.java[tags=point]
----

image::style-point.png[align="center", width="480"]

为 `PointFeature` 设置文本而非图像，可以直接在地图上的指定坐标处显示文本标注，从而为显示的数据提供清晰的上下文。

`PointFeature` 使用文本时，可以通过调整 `font`、`fill`、`textAlign` 等属性来自定义文本的外观。

下面的示例演示了如何为 `PointFeature` 设置文本标签而不是图像：

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/StyleView.java[tags=point-text]
----

image::style-point-text.png[align="center", width="479"]

== MarkerFeature 样式

如需修改标记的图标，可以参考下面的示例：

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/StyleView.java[tags=marker]
----

此外，根据地理对象属性自定义标记图标的示例在 xref:maps:dispay-markers.adoc#use-markers[使用自定义标记] 章节。 

image::style-marker.png[align="center", width="491"]

== LineStringFeature 样式

如需更改 `LineStringFeature` 的样式，可以定义一个 `LineStringStyle` 对象，设置所需的外观属性（如 `fill`、`stroke` 等），然后将该样式应用于 `LineStringFeature`。

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/StyleView.java[tags=lineString]
----

image::style-line-string.png[align="center", width="476"]

== PolygonFeature 样式

如需更改 `PolygonFeature` 的样式，可以定义一个 `PolygonStyle` 对象，设置所需的外观属性（如 `fill`、`stroke` 等），然后将该样式应用于 `PolygonFeature`。

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/StyleView.java[tags=polygon]
----

image::style-polygon.png[align="center", width="476"]

[[select-mode-styles]]
== 选择模式样式

矢量源中的每种要素类型在 xref:editing-geometries.adoc#select-mode[选择模式] 下都有默认的样式。这些样式在用户选择地图要素时自动应用。如需自定义选择模式的样式，每个要素提供了添加自定义样式的方法。

例如，下面是为 `PolygonFeature` 添加自定义选择模式样式的方法：

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/SelectStyleView.java[tags=select-style]
----

image::styles-select.gif[align="center",width="455"]

NOTE: 要将相同的选择样式应用于源中所有的要素，则需要在矢量层设置选择样式。

[[modify-mode-styles]]
== 编辑模式样式

矢量源提供了 xref:editing-geometries.adoc#modify-mode[编辑模式]。当编辑模式激活时，会在几何图形生成可以拖拽的点，从而可以编辑几何图形，例如添加新的顶点或更改现有的顶点位置。模式中还定义了这些生成的顶点的样式。

例如，下面是为多边形顶点设置自定义选择模式样式的方法：

[source,java,indent=0]
----
include::example$/maps-ex2/src/main/java/com/company/mapssample/view/style/ModifyStyleView.java[tags=modify-style]
----

image::styles-modify.gif[align="center",width="290"]

NOTE: 要将相同的选择样式应用于源中所有的要素，则需要在矢量层设置选择样式。
